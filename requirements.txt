# Runtime dependencies (used by CI and the Azure App Service / Oryx build).
# Includes agent framework, observability, and evaluation SDKs
# needed by the running app (telemetry, score tracking, safety agent).

# Multi-Agent Framework
agent-framework-azure-ai>=1.0.0rc2
agent-framework-core>=1.0.0rc2

# Azure SDKs
azure-identity>=1.25.3
# Pinned exactly: metric key names differ between versions, which breaks the baseline diff.
azure-ai-evaluation==1.18.5

# API
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
httpx>=0.27.0

# Observability
# azure-monitor-opentelemetry 1.8.6 requires opentelemetry-sdk==1.39 exactly
opentelemetry-api==1.39.0
opentelemetry-sdk==1.39.0
azure-monitor-opentelemetry==1.8.6
opentelemetry-instrumentation-fastapi==0.60b0
opentelemetry-instrumentation-httpx>=0.49b0

# Config
pydantic>=2.13.5
pydantic-settings>=2.15.0
python-dotenv>=1.2.3
